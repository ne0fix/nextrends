{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "PRISMA_SKIP_POSTINSTALL_GENERATE=1 pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @nextface/web build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" }
      ]
    }
  ]
}
